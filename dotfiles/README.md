## My configs collection

This is a repo designed to be cloned bare, and it sets up an alias `dotfiles` for adding any config file in `~` outside the repo.

I.e. `dotfiles add .vimrc`

### Note to self - installing

```
git clone --bare <repo>
dotfiles checkout
```
